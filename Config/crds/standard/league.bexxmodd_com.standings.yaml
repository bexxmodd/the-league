apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: standings.bexxmodd.com
spec:
  group: bexxmodd.com
  names:
    categories: []
    kind: Standing
    plural: standings
    shortNames: []
    singular: standing
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for StandingSpec via `CustomResource`
        properties:
          spec:
            description: |-
              Standing is the Schema for the Standing API.
              This resource tracks the calculated performance for a single Team.
            properties:
              leagueName:
                description: LeagueName references the parent TheLeague resource this standing belongs to.
                type: string
              resolution:
                description: Resolution defines the tie-breaking method used for calculating the standing.
                enum:
                - Head2Head
                - GoalDifference
                type: string
              teamName:
                description: TeamName is the name of the team this standing corresponds to.
                type: string
            required:
            - leagueName
            - resolution
            - teamName
            type: object
          status:
            description: |-
              StandingStatus defines the observed and computed state of the Standing.
              This field is managed by the controller.
            nullable: true
            properties:
              conditions:
                description: Conditions represent the latest available observations of the Standing's state.
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                nullable: true
                type: array
              draws:
                description: Draws is the total number of draws.
                format: uint32
                minimum: 0.0
                type: integer
              losses:
                description: Losses is the total number of losses.
                format: uint32
                minimum: 0.0
                type: integer
              points:
                description: Points is the total accumulated points for the team.
                format: uint32
                minimum: 0.0
                type: integer
              wins:
                description: Wins is the total number of wins.
                format: uint32
                minimum: 0.0
                type: integer
            required:
            - draws
            - losses
            - points
            - wins
            type: object
        required:
        - spec
        title: Standing
        type: object
    served: true
    storage: true
    subresources:
      status: {}
